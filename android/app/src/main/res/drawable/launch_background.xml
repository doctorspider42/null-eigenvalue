<?xml version="1.0" encoding="utf-8"?>
<!-- No image: see values/colors.xml. The app is dark in every theme, so the
     light-mode variant is the dark colour too rather than white. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/field_background" />
</layer-list>
