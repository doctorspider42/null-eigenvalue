// The synthesizer itself. Plain C++ - it is compiled as Objective-C++ only
// because it lives next to ne_api.mm and there is no reason to make the pod
// juggle two dialects.
#include "../../src/engine.cpp"
